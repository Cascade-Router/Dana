# Lean deps for the Hugging Face Space Docker image ONLY — see ../Dockerfile.
# Deliberately excludes the desktop-only stacks in the root requirements.txt
# (torch/transformers/audio/customtkinter/pywin32/...): this container always
# resolves dana.platform's Mock* drivers (SPACE_ID is set on every HF Space),
# which only need trimesh for headless CAD geometry.
fastapi==0.135.1
uvicorn[standard]==0.42.0
websockets==15.0.1
pillow>=10.0.0
pydantic>=2.0.0
numpy>=1.26.0
trimesh>=4.0.0
httpx>=0.27.0
